<?xml version="1.0" encoding="UTF-8"?>

<?import java.net.URL?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox spacing="10" styleClass="farm-root" xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.example.demo.CSController">
  <stylesheets>
    <URL value="@styles.css" />
  </stylesheets>
   <Label fx:id="clientID" styleClass="clientID-label" text="ID" />
  <!-- Top status bar: coins + status message -->
  <HBox alignment="CENTER_LEFT" spacing="10">
    <Label fx:id="coinsLabel" styleClass="coins-label" text="Coins: 0 | Ready." />
  </HBox>

  <!-- Game board grid (cells created by Controller) -->
  <GridPane fx:id="gameBoard" hgap="5" styleClass="game-board" vgap="5" VBox.vgrow="ALWAYS" />

  <!-- Action buttons -->
  <HBox alignment="CENTER" spacing="10">
    <Button fx:id="plantButton" onAction="#handlePlant" styleClass="button action-plant" text="Plant" />
    <Button fx:id="harvestButton" onAction="#handleHarvest" styleClass="button action-harvest" text="Harvest" />
    <Button fx:id="stealButton" onAction="#handleSteal" styleClass="button action-steal" text="Steal" />
  </HBox>
  <!-- players connected to the server-->
  <HBox fx:id="playersBox" alignment="CENTER" spacing="5">
    <Label text="Players:" styleClass="clientID-label" />
  </HBox>
</VBox>
